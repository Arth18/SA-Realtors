# Nothing in this folder should ever be fetched over the web.
# Apache 2.4 and LiteSpeed:
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
# Apache 2.2 fallback:
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
